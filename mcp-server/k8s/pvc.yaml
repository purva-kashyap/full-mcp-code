apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-token-cache
  namespace: mcp-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard  # Change based on your cluster
