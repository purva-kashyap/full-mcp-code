apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-web-app-config
  namespace: mcp-system
data:
  SECRET_KEY: "change-this-in-production-use-kubectl-create-secret"
  MCP_SERVER_URL: "http://mcp-server:8001/mcp"
  CALLBACK_BASE_URL: "http://mcp-server:8000"  # Update with your ingress domain
